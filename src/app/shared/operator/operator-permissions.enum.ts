export enum OperatorPermissions {
  allowPriceChange = 0,
  allowLineDiscount = 1,
  allowDocumentDiscount = 2,
  allowDocumentCancellation = 3,
  allowFuellingPointTest = 4,
  allowForecourtPriceChange = 5,
}
